//let close = function () {
    a = document.querySelectorAll('.nav-link');
    //let b = document.querySelector('.navbar-collapse').toggle('.show');

    for (let a = 0; a < a.length - 1; a++) {
        a[i].addEventListener('click', toggleNavbar());
    }
    
//}

//close();

function toggleNavbar() {
    console.log('test');
}